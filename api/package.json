{
  "name": "muslim-users-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "backup": "bun scripts/backup-db.ts",
    "backup:verify": "bun scripts/verify-backups.ts",
    "backup:prune": "bun scripts/prune-backups.ts",
    "restore": "bun scripts/restore-db.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
